The makefile assumes that the ARM toolchain is in ./.eabi. To launch a
simple demo use `make dump`; this will take the aes.sm in examples,
compile it with the ARM assembler and decompiling it with Capstone.
